-   id: apigentools-validate
    name: Validate OpenAPI specification files
    language: python
    entry: apigentools
    args:
      - validate
    description: Validate OpenAPI specification files generated by apigentools using configuration inside this repository.
    pass_filenames: false
# left here for historical purposes, `apigentools-validate` is now containerized because `apigentools` itself is containerized
-   id: container-apigentools-validate
    name: "DEPRECATED: use 'apigentools-validate'"
    language: python
    entry: container-apigentools
    args:
      - validate
    description: "DEPRECATED: use 'apigentools-validate'"
    pass_filenames: false
